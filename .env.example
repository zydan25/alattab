# =====================================
# إعدادات قاعدة البيانات
# =====================================

# ==== الطريقة الأولى: رابط قاعدة البيانات المباشر (الأسهل) ====
# يمكنك وضع رابط قاعدة البيانات كاملاً هنا مباشرة
# إذا تم تعيين DATABASE_URI، سيتم تجاهل جميع الإعدادات الأخرى

# DATABASE_URI=sqlite:///instance/balance.db
# DATABASE_URI=mysql+pymysql://user:password@localhost:3306/whatsapp_bot?charset=utf8mb4
# DATABASE_URI=postgresql+psycopg2://user:password@localhost:5432/whatsapp_bot

# ==== الطريقة الثانية: إعدادات منفصلة ====
# نوع قاعدة البيانات: sqlite, mysql, postgresql
DATABASE_TYPE=sqlite

# --- إعدادات SQLite (افتراضي) ---
SQLITE_DB_PATH=instance/balance.db

# --- إعدادات MySQL ---
# DATABASE_TYPE=mysql
# DATABASE_HOST=localhost
# DATABASE_PORT=3306
# DATABASE_USER=root
# DATABASE_PASSWORD=your_mysql_password
# DATABASE_NAME=whatsapp_bot
# DATABASE_CHARSET=utf8mb4

# --- إعدادات PostgreSQL ---
# DATABASE_TYPE=postgresql
# DATABASE_HOST=localhost
# DATABASE_PORT=5432
# DATABASE_USER=postgres
# DATABASE_PASSWORD=your_postgres_password
# DATABASE_NAME=whatsapp_bot

# --- إعدادات Connection Pool (اختياري) ---
DB_POOL_SIZE=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_MAX_OVERFLOW=20

# =====================================
# إعدادات التطبيق
# =====================================

# مفتاح الجلسة (يجب تغييره في الإنتاج)
SECRET_KEY=your-secret-key-change-this-in-production

# بيانات تسجيل دخول المدير
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# =====================================
# إعدادات API الخارجي
# =====================================
# (تُحمل من config.json حالياً)
